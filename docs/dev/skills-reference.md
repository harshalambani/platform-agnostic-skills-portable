<!-- GENERATED by scripts/gen_docs.py — do not edit by hand. Edit the skill's skill.yaml `help:` block and re-run `python scripts/gen_docs.py`. -->
# Skills Reference (developer)

Auto-generated inventory of every skill. UI skills appear as tabs; internal steps run only inside the GnuCash Import pipeline.

## UI skills

| Skill | Dir | Mode | LLM | Inputs | Output | Native deps |
|-------|-----|------|-----|--------|--------|-------------|
| Bank of Baroda | `skill_bob` | direct | no | Bank of Baroda statement PDF(s) (PDF (.pdf) — one or many, in any order.) | `YYYY-MM-DD-HHMMSS-<input>-BoB.csv` | — |
| CSV Data Analyzer | `skill_csv_analyzer` | agent | yes | CSV file (CSV (.csv) with a header row.); Question about this data (e.g. 'What is the average revenue by region?') (Free text.) | `YYYY-MM-DD-HHMMSS-<input>-analysis.md` | — |
| Convert | `skill_26as` | direct | no | Form 26AS PDF (PDF (.pdf) — a single TRACES-issued 26AS.) | `YYYY-MM-DD-HHMMSS-<input>-26AS.xlsx` | — |
| Credit Card — Sort PDFs | `skill_cc_sort` | direct | no | Input folder (MSG files and/or PDFs) (A folder of .msg and/or .pdf files.); PDF password(s), comma-separated (or leave blank for auto-detect) (Text — one password, or several comma-separated. Optional.) | `` | qpdf |
| Credit Card — Transactions | `skill_cc_transactions` | direct | no | Organized PDFs folder (Decrypted_PDFs_Correct/ from CC Sort) (A folder of decrypted statement PDFs.) | `YYYY-MM-DD-HHMMSS-<input>-CC-Transactions.xlsx` | — |
| Document Summarizer | `skill_summarize` | direct | yes | Document (PDF or text file) (PDF (.pdf) or text (.txt, .md, .csv, .log, .json, .xml, .html) — one file.) | `YYYY-MM-DD-HHMMSS-<input>-summary.md` | — |
| GnuCash Import | `skill_krc_gnucash` | direct | no | Part II Bills workbook (Excel (.xlsx) matching *-KRC-Bills-Recon.xlsx.); GnuCash book (.gnucash) (GnuCash file (.gnucash).) | `` | — |
| HDFC | `skill_hdfc` | direct | no | HDFC statement (PDF, XLS, or XLSX) (PDF (.pdf), Excel (.xls / .xlsx) — one file.) | `YYYY-MM-DD-HHMMSS-<input>-HDFC_canonical.csv` | tesseract |
| HSBC | `skill_hsbc` | agent | yes | Folder containing HSBC statement PDF(s) (A folder of HSBC PDF statements.) | `YYYY-MM-DD-HHMMSS-<input>-HSBC.xlsx` | tesseract, poppler |
| ICICI | `skill_icici` | agent | yes | ICICI statement XLS file(s) (Excel 97-2003 (.xls) — one or many.) | `YYYY-MM-DD-HHMMSS-<input>-ICICI_GnuCash.csv` | — |
| Import Statement | `skill_gnucash_pipeline` | agent | yes | Bank (One of: ICICI, Bank of Baroda, HSBC, HDFC, Other Bank (CSV).); Statement file(s) — XLS for ICICI; PDF(s) for BoB / HSBC; CSV or XLS/XLSX for HDFC / Other (XLS for ICICI; PDF(s) for BoB / HSBC; CSV or XLS/XLSX for HDFC / Other.); GnuCash book (.gnucash) — must be closed in GnuCash (GnuCash file (.gnucash).) | `YYYY-MM-DD-HHMMSS-<input>-GnuCash_import_ready.csv` | — |
| Journal | `skill_26as_journal` | agent | yes | 26AS workbook (Convert output) (Excel (.xlsx) matching *-26AS.xlsx.); GnuCash file (GnuCash file (.gnucash).) | `YYYY-MM-DD-HHMMSS-<input>-tds-journals.csv` | — |
| KRChoksey | `skill_krc` | direct | no | KR Choksey Ledger Statement PDF (PDF (.pdf) — the ledger statement.); PDF password (account holder's PAN) (Text — the account holder's PAN.) | `YYYY-MM-DD-HHMMSS-<input>-KRC-Ledger.xlsx` | qpdf |
| MSG / Email Parser | `skill_msg_parser` | direct | no | Email file (.msg or .eml) (Outlook .msg or RFC-822 .eml — one file.) | `YYYY-MM-DD-HHMMSS-<input>-email-parsed.json` | — |
| Parser Generator | `skill_parser_generator` | agent | yes | Task (One of: Fix a failing parser, Create a new parser.); Parser (pick a known parser to Fix, or type a new path to Create) (A known parser (pick from the list) or a new parse_<format>.py path.); Tie-out args (Fix only: the parser's CLI args, e.g. 'statement.pdf PASSWORD out.xlsx') (Text, e.g. 'statement.pdf PASSWORD out.xlsx'. Optional.); Notes (Fix: the symptom / expected closing balance; Create: the format / FORMAT_NAME) (Text. Fix: the symptom / expected closing balance. Create: the format / FORMAT_NAME. Optional.) | `` | — |
| Reconcile | `skill_krc_recon` | direct | no | Part I Simplified Ledger workbook (Excel (.xlsx) matching *-KRC-Ledger.xlsx.); Contract Notes (SLBM memos + trade notes) (PDF (.pdf) — one or many contract notes.); PDF password (account holder's PAN) (Text — the account holder's PAN.) | `YYYY-MM-DD-HHMMSS-<input>-KRC-Bills-Recon.xlsx` | qpdf |
| Text Translator | `skill_translate` | direct | yes | Text to translate (Free text.); Source language (auto, English, Hindi, Spanish, French, German, Japanese, Chinese, Arabic, Portuguese, Russian.); Target language (English, Hindi, Spanish, French, German, Japanese, Chinese, Arabic, Portuguese, Russian.) | `YYYY-MM-DD-HHMMSS-<input>-translation.txt` | — |

## Internal pipeline steps

Not registered as UI tabs (no `display_name`). Used programmatically by `skill_gnucash_pipeline`.

| Step (dir) | Category | LLM | What it does |
|------------|----------|-----|--------------|
| `skill_gnucash_account_mapper` | gnucash | no | Build account-mapping rules from your GnuCash book, then apply them to a canonical CSV. Chains: XML extraction → recency-weighted rule generation → account mapping. No LLM required — fully deterministic. |
| `skill_gnucash_import` | gnucash | no | Convert bank/financial statements (CSV/XLSX) to GnuCash-importable CSV format. Uses deterministic pre-parse + LLM spec-generation + deterministic row transform. |
| `skill_gnucash_mapping_generator` | gnucash | no | Generate mapping rules (YAML) from extracted GnuCash data. Creates categorized rules (by bank) with confidence levels. |
| `skill_gnucash_mapping_validator` | gnucash | no | Test mapping rules against a bank statement to measure accuracy. Validates that your rules correctly predict GnuCash accounts. |
| `skill_gnucash_reconciler` | gnucash | no | Compare a canonical CSV against your GnuCash book before importing. Flags: duplicates (date+amount match), balance gaps, and missing transactions. Read-only — never modifies your .gnucash file. Run this after mapping accounts and before GnuCash import to catch issues early. |
| `skill_gnucash_xml_extractor` | gnucash | no | Parse a GnuCash (.gnucash) file and extract account hierarchy + transaction mappings. Outputs JSON with all description→account patterns from your financial history. |
