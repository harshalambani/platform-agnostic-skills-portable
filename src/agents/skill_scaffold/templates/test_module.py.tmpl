"""
tests/test_{{SKILL_DIR}}.py -- scaffolded test for {{DISPLAY_NAME}}.

TODO(scaffold): this test module was generated by the Skill Scaffolder
(agents.skill_scaffold). Replace this placeholder test with real coverage
before this skill ships -- it only checks that the stub imports and runs.
"""
import importlib
import sys
from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent.parent
SRC = PROJECT_ROOT / "src"
if str(SRC) not in sys.path:
    sys.path.insert(0, str(SRC))


def test_{{SKILL_DIR}}_imports():
    mod = importlib.import_module("agents.{{SKILL_DIR}}.agent")
    assert hasattr(mod, "{{ENTRY_FUNC}}")


def test_{{SKILL_DIR}}_stub_runs(tmp_path):
    # TODO(scaffold): replace with a real test once the skill is implemented.
    mod = importlib.import_module("agents.{{SKILL_DIR}}.agent")
    fn = getattr(mod, "{{ENTRY_FUNC}}")
    out = tmp_path / "out"
    result = fn(input_path="", output_path=str(out))
    assert isinstance(result, str)
    assert result
