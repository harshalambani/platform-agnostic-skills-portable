# skill.yaml -- {{DISPLAY_NAME}} manifest.
#
# Scaffolded by the Skill Scaffolder (agents.skill_scaffold). TODO(scaffold):
# replace this placeholder manifest with the real one once the skill is
# implemented -- see AGENT.md and agent.py in this package.

name: "{{NAME}}"
display_name: "{{DISPLAY_NAME}}"
description: >
  TODO(scaffold): one/two sentence description of what this skill does and
  why it exists. Placeholder generated by the Skill Scaffolder -- replace
  before shipping.
category: "{{CATEGORY}}"
version: "0.1.0"

mode: "{{MODE}}"
entry_point: "{{ENTRY_POINT}}"

inputs:
  - name: "input_path"
    type: "text"
    label: "TODO(scaffold): input label"
    required: true

run_args:
  input_path: "{inputs.input_path}"
  output_path: "{output_path}"
  config_path: "{config_path}"
  model_override: "{model_override}"

output:
  type: "{{OUTPUT_TYPE}}"
  suffix: "{{OUTPUT_SUFFIX}}"
  extension: "{{OUTPUT_EXTENSION}}"
  download_label: "Download {{DISPLAY_NAME}}"

requires:
  native_binaries: []
  external_tools: []
  llm: {{NEEDS_LLM}}
  network: false

help:
  overview: >
    TODO(scaffold): describe, in plain language, what this skill does and the
    problem it solves. Placeholder generated by the Skill Scaffolder.
  when_to_use: >
    TODO(scaffold): describe when a user should reach for this skill instead
    of another one.
  inputs:
    - name: "input_path"
      tooltip: "TODO(scaffold): short tooltip for this input."
      accepts: "TODO(scaffold): what file types / text format this input accepts."
      gotchas: "TODO(scaffold): anything a user could get wrong here."
  steps:
    - "TODO(scaffold): step 1."
    - "TODO(scaffold): step 2."
  outputs:
    folder: "Data/outputs/"
    files:
      - name: "TODO(scaffold): output file/folder name pattern"
        tooltip: "TODO(scaffold): what this output contains."
  tips: >
    TODO(scaffold): any tips worth surfacing to a user.
  troubleshooting:
    - problem: "TODO(scaffold): a problem a user might hit."
      fix: "TODO(scaffold): how to fix it."
