# skill_gnucash_account_mapper — internal pipeline step.
# NOT registered as a UI skill — used programmatically by skill_gnucash_pipeline.
# To restore as a standalone tab, add: name, display_name, mode, entry_point fields.

description: >
  Build account-mapping rules from your GnuCash book, then apply them to a canonical CSV.
  Chains: XML extraction → recency-weighted rule generation → account mapping.
  No LLM required — fully deterministic.
category: "gnucash"
version: "1.0.0"
