# adapter_bob — internal BoB→Canonical pipeline step.
# NOT registered as a UI skill — used programmatically by pipeline skills.
# To restore as a standalone tab, add: name, mode, entry_point fields.

description: >
  Convert Bank of Baroda (BoB skill) native CSV to the canonical 8-column schema.
  Called internally by GnuCash Import Pipeline. No LLM required.
  Columns: DATE, PARTICULARS, CHQ.NO., WITHDRAWALS, DEPOSITS, BALANCE
  Output: Date, Transaction ID, Description, Account, Deposit, Withdrawal, Balance, Currency
version: "1.0.0"
